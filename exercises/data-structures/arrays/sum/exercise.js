
/**
 * Computes the sum of the elements of the array using a for loop and returns it.
 * @param {number[]} arr 
 * @returns number
 */
function sumWithFor(arr) {
    // WRITE YOUR CODE HERE
}

/**
 * Computes the sum of the elements of the given array `forEach` method and returns it.
 * @param {number[]} arr 
 * @returns number
 */
function sumWithForEach(arr) {
    // WRITE YOUR CODE HERE
}

/**
 * Computes the sum of the elements of the given array `reduce` method and returns it.
 * @param {number[]} arr 
 * @returns number
 */
function sumWithReduce(arr) {
    // WRITE YOUR CODE HERE
}

// Do not touch anything above this.
module.exports = {
    sumWithFor,
    sumWithForEach,
    sumWithReduce
}